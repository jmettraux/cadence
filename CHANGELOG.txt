
## CHANGELOG.txt

* Add _is_point(p)
* Add _to_spherical_xyz() and _to_ele_dir()
* Add _sindex(s, s1) returns undef or the index of s1 in s
* Add _sstr(s, from=0, length=undef)
* Add _app(arr, entry) to complement _assoc()
* Add _assoc(arr, key, default, off=0)
* Unlock _bezier_points 3D
* Add _idx, _slist list functions
* Add point functions
* Add _get, _put, _del associative array functions
* Introduce dome
* Introduce paslice (pineapple slice)

